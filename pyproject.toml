[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "fastapi-host-filter"
version = "0.1.0"
description = "FastAPI middleware to prevent HTTP Host Header Poisoning attacks with wildcard subdomain support"
readme = "README.md"
requires-python = ">=3.10"
license = "MIT"
authors = [
    { name = "me1121118" }
]
keywords = ["fastapi", "python", "utility", "tools"]
classifiers = [
    "Programming Language :: Python :: 3",
    "License :: OSI Approved :: MIT License",
    "Operating System :: OS Independent",
]
dependencies = ["fastapi>=0.100.0", "starlette>=0.27.0"]

[project.urls]
Homepage = "https://github.com/me1121118/fastapi-host-filter"
Repository = "https://github.com/me1121118/fastapi-host-filter"
